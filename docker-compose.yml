services:
  nostr-oidc:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nostr-oidc
    ports:
      - "8082:8082"
    env_file:
      - .env
    volumes:
      # Persist database (database.db will be created in /app/data)
      - ./data:/app/data
      # Mount .env file for godotenv to read
      - ./.env:/app/.env:ro
    cap_add:
      - IPC_LOCK
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8082/"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
